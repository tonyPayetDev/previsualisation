
# Cache brut de la requête OpenStreetMap (~500 Ko, réécrit à chaque
# génération). Il sert de secours quand les miroirs Overpass tombent — c'est
# une donnée locale, pas une source. Le versionner ajouterait un demi-Mo par
# passage à un dépôt dont le gc meurt déjà en manque de mémoire.
leads-restaurants/osm-brut.json
